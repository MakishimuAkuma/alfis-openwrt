name: Build Alfis OpenWrt APK

on:
  release:
    types:
      - published

  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing GitHub Release tag"
        required: true
        type: string

permissions:
  contents: write

env:
  # OpenWrt release branch (used only for display / checkout references).
  OPENWRT_BRANCH: openwrt-25.12

  # OpenWrt SDK Docker image tag suffix.
  # IMPORTANT: openwrt/sdk images on Docker Hub are tagged as
  # "<arch>-<version>" using the plain release VERSION (e.g. "25.12.4"),
  # NOT the git branch name ("openwrt-25.12"). Using the branch name here
  # produces a tag that does not exist and "docker pull" fails for every
  # matrix job. Update this whenever you bump to a new point release.
  OPENWRT_VERSION: 25.12.4


jobs:

  # ============================================================
  # BUILD ALFIS
  # ============================================================

  build-alfis:
    name: Build Alfis (${{ matrix.arch }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false

      matrix:
        arch:
          - arm_cortex-a7
          - arm_cortex-a9
          - arm_cortex-a15_neon-vfpv4

          - aarch64_cortex-a53
          - aarch64_cortex-a72
          - rockchip-armv8
          - aarch64_generic

          - i386_pentium4
          - x86_64

          - mipsel_24kc
          - mips_mips32
          - mips64_octeonplus

          - riscv64_generic

    steps:

      # ----------------------------------------------------------
      # Checkout
      # ----------------------------------------------------------

      - name: Checkout package feed
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1


      # ----------------------------------------------------------
      # Verify package
      # ----------------------------------------------------------

      - name: Verify Alfis package
        shell: bash
        run: |
          set -e

          echo "========================================"
          echo "Package:      Alfis"
          echo "Architecture: ${{ matrix.arch }}"
          echo "OpenWrt:      ${{ env.OPENWRT_BRANCH }} (SDK ${{ env.OPENWRT_VERSION }})"
          echo "========================================"

          test -f alfis/Makefile
          test -f alfis/files/alfis.init
          test -f alfis/files/alfis.config

          echo
          echo "Package metadata:"
          grep -E \
            '^PKG_(NAME|VERSION|RELEASE|SOURCE|SOURCE_URL|SOURCE_SUBDIR|HASH):' \
            alfis/Makefile


      # ----------------------------------------------------------
      # Build
      # ----------------------------------------------------------

      - name: Build OpenWrt Alfis package
        uses: openwrt/gh-action-sdk@main
        env:
          ARCH: ${{ matrix.arch }}-${{ env.OPENWRT_VERSION }}

          FEEDNAME: action
          PACKAGES: alfis

          NO_REFRESH_CHECK: 1
          NO_SHFMT_CHECK: 1

          BUILD_LOG: 1


      # ----------------------------------------------------------
      # Find APK
      # ----------------------------------------------------------

      - name: Find Alfis APK
        id: apk
        shell: bash
        run: |
          set -e

          APK="$(find bin \
            -type f \
            -name 'alfis-*.apk' \
            | head -n 1)"

          if [ -z "$APK" ]; then
            echo "ERROR: Alfis APK not found"

            echo
            echo "Available APK files:"
            find bin \
              -type f \
              -name '*.apk' \
              -print \
              | sort || true

            exit 1
          fi

          echo
          echo "Found Alfis APK:"
          echo "$APK"

          echo "source=$APK" >> "$GITHUB_OUTPUT"


      # ----------------------------------------------------------
      # Rename APK
      # ----------------------------------------------------------

      - name: Rename Alfis APK
        id: renamed
        shell: bash
        run: |
          set -e

          SOURCE="${{ steps.apk.outputs.source }}"

          DIR="$(dirname "$SOURCE")"
          BASE="$(basename "$SOURCE" .apk)"

          TARGET="${DIR}/${BASE}-${{ matrix.arch }}.apk"

          mv "$SOURCE" "$TARGET"

          echo "Renamed:"
          echo "$TARGET"

          echo "path=$TARGET" >> "$GITHUB_OUTPUT"


      # ----------------------------------------------------------
      # Upload artifact
      # ----------------------------------------------------------

      - name: Upload Alfis APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: alfis-apk-${{ matrix.arch }}
          path: ${{ steps.renamed.outputs.path }}
          if-no-files-found: error
          retention-days: 7


  # ============================================================
  # BUILD LUCI
  # ============================================================

  build-luci:
    name: Build LuCI
    runs-on: ubuntu-latest

    steps:

      # ----------------------------------------------------------
      # Checkout
      # ----------------------------------------------------------

      - name: Checkout package feed
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 1


      # ----------------------------------------------------------
      # Verify packages
      # ----------------------------------------------------------

      - name: Verify LuCI and Alfis packages
        shell: bash
        run: |
          set -e

          echo "========================================"
          echo "Packages:"
          echo "  - alfis"
          echo "  - luci-app-alfis"
          echo "Architecture: x86_64"
          echo "OpenWrt:      ${{ env.OPENWRT_BRANCH }} (SDK ${{ env.OPENWRT_VERSION }})"
          echo "========================================"

          test -f alfis/Makefile
          test -f alfis/files/alfis.init
          test -f alfis/files/alfis.config

          test -f luci-app-alfis/Makefile

          test -f \
            luci-app-alfis/htdocs/luci-static/resources/view/alfis/alfis.js

          test -f \
            luci-app-alfis/root/usr/share/rpcd/acl.d/luci-app-alfis.json

          test -f \
            luci-app-alfis/root/usr/share/luci/menu.d/luci-app-alfis.json

          echo
          echo "Alfis metadata:"
          grep -E \
            '^PKG_(NAME|VERSION|RELEASE|SOURCE|SOURCE_URL|SOURCE_SUBDIR|HASH):' \
            alfis/Makefile || true

          echo
          echo "LuCI metadata:"
          grep -E \
            '^(LUCI_TITLE|LUCI_DEPENDS|LUCI_PKGARCH|PKG_LICENSE|PKG_MAINTAINER):' \
            luci-app-alfis/Makefile || true


      # ----------------------------------------------------------
      # Build LuCI + Alfis
      # ----------------------------------------------------------

      - name: Build OpenWrt LuCI package
        uses: openwrt/gh-action-sdk@main
        env:
          ARCH: x86_64-${{ env.OPENWRT_VERSION }}

          FEEDNAME: action

          # IMPORTANT:
          # Build Alfis together with LuCI.
          #
          # luci-app-alfis contains:
          #
          #   LUCI_DEPENDS:=+alfis
          #
          # Therefore Alfis must be available in this SDK build.
          PACKAGES: alfis luci-app-alfis

          NO_REFRESH_CHECK: 1
          NO_SHFMT_CHECK: 1

          BUILD_LOG: 1


      # ----------------------------------------------------------
      # Find LuCI APK
      # ----------------------------------------------------------

      - name: Find LuCI APK
        id: apk
        shell: bash
        run: |
          set -e

          APK="$(find bin \
            -type f \
            -name 'luci-app-alfis-*.apk' \
            | head -n 1)"

          if [ -z "$APK" ]; then
            echo "ERROR: LuCI APK not found"

            echo
            echo "Available APK files:"
            find bin \
              -type f \
              -name '*.apk' \
              -print \
              | sort || true

            exit 1
          fi

          echo
          echo "Found LuCI APK:"
          echo "$APK"

          echo "source=$APK" >> "$GITHUB_OUTPUT"


      # ----------------------------------------------------------
      # Upload LuCI artifact
      # ----------------------------------------------------------

      - name: Upload LuCI APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: luci-app-alfis-apk
          path: ${{ steps.apk.outputs.source }}
          if-no-files-found: error
          retention-days: 7


  # ============================================================
  # RELEASE
  # ============================================================

  release:
    name: Publish APKs to GitHub Release

    needs:
      - build-alfis
      - build-luci

    runs-on: ubuntu-latest

    steps:

      # ----------------------------------------------------------
      # Determine Release tag
      # ----------------------------------------------------------

      - name: Determine release tag
        id: release_tag
        shell: bash
        env:
          EVENT_TAG: ${{ github.event.release.tag_name }}
          MANUAL_TAG: ${{ inputs.release_tag }}
        run: |
          set -e

          if [ -n "$EVENT_TAG" ]; then
            TAG="$EVENT_TAG"
          else
            TAG="$MANUAL_TAG"
          fi

          if [ -z "$TAG" ]; then
            echo "ERROR: Release tag is empty"
            exit 1
          fi

          echo "Using Release:"
          echo "$TAG"

          echo "tag=$TAG" >> "$GITHUB_OUTPUT"


      # ----------------------------------------------------------
      # Download Alfis artifacts
      # ----------------------------------------------------------

      - name: Download Alfis APK artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: alfis-apk-*
          path: release
          merge-multiple: true


      # ----------------------------------------------------------
      # Download LuCI artifact
      # ----------------------------------------------------------

      - name: Download LuCI APK artifact
        uses: actions/download-artifact@v4
        with:
          name: luci-app-alfis-apk
          path: release


      # ----------------------------------------------------------
      # List APKs
      # ----------------------------------------------------------

      - name: List APKs
        shell: bash
        run: |
          set -e

          echo "========================================"
          echo "APKs to upload"
          echo "========================================"

          find release \
            -type f \
            -name '*.apk' \
            -print \
            | sort

          COUNT="$(find release -type f -name '*.apk' | wc -l)"

          echo
          echo "Found $COUNT APK(s)"

          if [ "$COUNT" -ne 14 ]; then
            echo
            echo "ERROR: Expected 14 APKs"
            echo "       13 Alfis + 1 LuCI"

            exit 1
          fi


      # ----------------------------------------------------------
      # Verify expected packages
      # ----------------------------------------------------------

      - name: Verify APK set
        shell: bash
        run: |
          set -e

          EXPECTED_ARCHS="
          arm_cortex-a7
          arm_cortex-a9
          arm_cortex-a15_neon-vfpv4
          aarch64_cortex-a53
          aarch64_cortex-a72
          rockchip-armv8
          aarch64_generic
          i386_pentium4
          x86_64
          mipsel_24kc
          mips_mips32
          mips64_octeonplus
          riscv64_generic
          "

          for ARCH in $EXPECTED_ARCHS; do
            if ! find release \
              -type f \
              -name "alfis-*-${ARCH}.apk" \
              | grep -q .; then

              echo "ERROR: Missing Alfis APK for ${ARCH}"
              exit 1
            fi
          done

          if ! find release \
            -type f \
            -name 'luci-app-alfis-*.apk' \
            | grep -q .; then

            echo "ERROR: Missing luci-app-alfis APK"
            exit 1
          fi

          echo
          echo "All expected APKs are present."


      # ----------------------------------------------------------
      # Upload APKs to Release
      # ----------------------------------------------------------

      - name: Upload APKs to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release_tag.outputs.tag }}
          files: |
            release/*.apk
          fail_on_unmatched_files: true
          overwrite_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
